# Mock Mode Environment Configuration
# Copy this file to .env.local for local development with mock mode
# This is the MINIMAL configuration needed for mock mode - no AWS credentials required!

# ============================================================================
# REQUIRED FOR MOCK MODE
# ============================================================================

# Backend mode: "mock" for offline development, "aws" for real AWS
MC_BACKEND_MODE=mock

# Enable dev login route for local authentication (no Google OAuth needed)
# Visit http://localhost:3000/api/auth/dev-login to get a session cookie
ENABLE_DEV_LOGIN=true

# ============================================================================
# AUTHENTICATION (Required for dev login)
# ============================================================================

# Secret for signing JWT tokens (can be any random string for dev)
# In production, use a secure 32+ character random string
AUTH_SECRET=dev-secret-change-in-production

# App URL (for redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================================================
# OPTIONAL MOCK MODE SETTINGS
# ============================================================================

# Path for mock state persistence file (optional)
# Uncomment to save mock state to file for debugging
# MOCK_STATE_PATH=./mock-state.json

# Default scenario to apply on startup (optional)
# Uncomment to auto-apply a scenario when the server starts
# MOCK_SCENARIO=default

# ============================================================================
# AWS CREDENTIALS (NOT REQUIRED IN MOCK MODE)
# ============================================================================
# The following variables are NOT needed when MC_BACKEND_MODE=mock
# They are only required when using real AWS infrastructure

# AWS_REGION=us-west-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# INSTANCE_ID=

# ============================================================================
# OTHER CONFIGURATION (NOT REQUIRED FOR MOCK MODE)
# ============================================================================
# These are only needed for production or when using real AWS

# Google OAuth (not needed with dev login)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# Email configuration
# ADMIN_EMAIL=
# ALLOWED_EMAILS=

# Cloudflare DNS
# CLOUDFLARE_ZONE_ID=
# CLOUDFLARE_API_TOKEN=
# CLOUDFLARE_RECORD_ID=
# CLOUDFLARE_MC_DOMAIN=

# SES email configuration
# VERIFIED_SENDER=
# NOTIFICATION_EMAIL=
# START_KEYWORD=startserver123

# GitHub (for server config sync)
# GITHUB_USER=
# GITHUB_REPO=
# GITHUB_TOKEN=

# Google Drive backups
# GDRIVE_REMOTE=gdrive
# GDRIVE_ROOT=mc-backups