# Environment Configuration
# Copy this file to both .env.local and .env.production, then fill in your values
# Local development uses .env.local, deployment uses .env.production

# AWS Configuration
# Get these from: docs/AWS_CREDENTIALS_SETUP.md
AWS_REGION=us-west-1
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# AWS CDK (optional, usually auto-detected)
CDK_DEFAULT_ACCOUNT=123456789012
CDK_DEFAULT_REGION=us-west-1

# Google OAuth
# Get these from: docs/GOOGLE_OAUTH_SETUP.md
# Add both redirect URIs to Google Console:
#   - http://localhost:3000/api/auth/callback
#   - https://panel.yourdomain.com/api/auth/callback
GOOGLE_CLIENT_ID=123456789-abcdefg.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-abc123xyz789

# Authentication & Authorization
# Admin user (full access)
ADMIN_EMAIL=your-email@gmail.com

# Allowed users (can start server, view status)
ALLOWED_EMAILS=friend1@gmail.com,friend2@gmail.com

# Production Settings
# For local development, these are set automatically by package.json scripts
# For production, you MUST set these:

# Your production control-panel URL (REQUIRED for production)
NEXT_PUBLIC_APP_URL=https://panel.yourdomain.com

# JWT signing secret (auto-generated if missing/placeholder)
AUTH_SECRET=your-secret-here

# Cloudflare DNS
# For auto-updating Minecraft server IP when instance starts
# Cloudflare DNS API token (renamed from CLOUDFLARE_API_TOKEN to avoid wrangler auth collision)
CLOUDFLARE_DNS_API_TOKEN=your-cloudflare-api-token
CLOUDFLARE_ZONE_ID=your-zone-id
CLOUDFLARE_RECORD_ID=your-record-id
CLOUDFLARE_MC_DOMAIN=mc.yourdomain.com

# Runtime-state Cloudflare KV namespace IDs (used by deploy script)
# Create with:
#   pnpm exec wrangler kv namespace create RUNTIME_STATE_SNAPSHOT_KV
#   pnpm exec wrangler kv namespace create RUNTIME_STATE_SNAPSHOT_KV --preview
RUNTIME_STATE_SNAPSHOT_KV_ID=your-runtime-state-kv-id
# Optional: defaults to RUNTIME_STATE_SNAPSHOT_KV_ID when omitted
RUNTIME_STATE_SNAPSHOT_KV_PREVIEW_ID=your-runtime-state-kv-preview-id

# Email Configuration (SES)
# For email-to-start Lambda function
VERIFIED_SENDER=start@yourdomain.com
NOTIFICATION_EMAIL=your-email@gmail.com
START_KEYWORD=startserver123

# GitHub Integration
# For server config synchronization
GITHUB_USER=yourusername
GITHUB_REPO=yourrepo
GITHUB_TOKEN=ghp_yourtoken

# Google Drive Backups
GDRIVE_REMOTE=gdrive
GDRIVE_ROOT=mc-backups

# Key pair for EC2 SSH access
KEY_PAIR_NAME=mc-aws

# Notes:
# MC_BACKEND_MODE, ENABLE_DEV_LOGIN are set by package.json scripts:
#   - pnpm dev          → aws mode, dev login enabled, localhost
#   - pnpm dev:mock     → mock mode, dev login enabled, localhost
#   - pnpm deploy:cf    → aws mode, dev login disabled, uses NEXT_PUBLIC_APP_URL above
