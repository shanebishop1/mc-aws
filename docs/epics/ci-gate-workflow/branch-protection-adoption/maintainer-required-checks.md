# Branch Protection: Required Baseline Check

Use the following **exact** status check name when configuring required checks for pull requests to the protected default branch:

- `Baseline PR Validation`

Source of truth: `.github/workflows/baseline-pr-validation.yml` → `jobs.baseline-pr-validation.name`.
