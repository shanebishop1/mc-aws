# Minecraft status report

Use `example.status-report.read` only for operator-selected, workspace-relative status files. Summarize the result
with evidence references. Never request writes, shell or console execution, host paths, cloud APIs, metadata, or
credential material. A hook reference is declarative metadata and is not executable extension code.
