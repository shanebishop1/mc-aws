# Security
  
  This is a personal project shared as-is.
  
  Never include AWS, Cloudflare, OAuth, Google Drive, or session credentials in issues or logs. Keep deployment credentials separate from the Worker runtime identity and use least-privilege access.
  
  See `docs/AWS_CREDENTIALS_SETUP.md` and `docs/TEARDOWN.md` for credential and resource-safety guidance.
  